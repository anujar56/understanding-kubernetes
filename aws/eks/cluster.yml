apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: new-cluster
  region: ap-south-1
  version: "1.34"
managedNodeGroups:
  - name: ng-1
    instanceType: t3.medium
    minSize: 1
    maxSize: 2
    desiredCapacity: 1
    volumeSize: 20
    iam:
      withAddonPolicies:
        autoScaler: true

